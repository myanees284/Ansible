- hosts: tomcat
  become: True
  tasks:
    - name: Create a group
      group:
        name: admins
        state: absent
    - name: Create user
      user:
        name: "{{ item.name }}"
        group: "{{ item.group }}"
        state: absent
      loop:
        - {name: 'anees', group: 'root'}
        - {name: 'tina', group: 'admins'}
